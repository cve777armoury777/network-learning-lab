# Routing Table

A routing table is a database used by routers to decide where to forward packets.

Routers check the destination IP address of a packet and use the routing table to find the best path.

## How Routing Works

Example: